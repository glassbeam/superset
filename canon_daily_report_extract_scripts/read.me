Update the `config.json` file with  Vertica and SMTP details.
Then, schedule the extract to run in the background using the following command:
nohup python3 -u canon_dailyreport_extract.py > canon_dailyreport_extract.log 2>&1 &
